services:
  - type: web
    name: intelligent-land-record-api
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: TESSERACT_CMD
        value: /usr/bin/tesseract
      - key: FRONTEND_URL
        sync: false
